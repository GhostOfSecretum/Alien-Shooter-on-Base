# 🔄 Принудительный передеплой на Vercel

## Проблема
Metadata пусты в Base Preview, но manifest верный. Возможна проблема с кешем или передеплоем.

## Решение 1: Принудительный передеплой через Vercel Dashboard

### Шаг 1: Откройте Vercel Dashboard
1. Перейдите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Найдите проект **"Three-in-a-row"** (или как он называется)
3. Откройте проект

### Шаг 2: Передеплой
1. Перейдите на вкладку **"Deployments"**
2. Найдите последний деплой
3. Нажмите на **три точки** (⋮) справа от деплоя
4. Выберите **"Redeploy"**
5. Подтвердите передеплой

### Шаг 3: Дождитесь завершения
- Обычно занимает 1-2 минуты
- Статус изменится на "Building" → "Ready"

---

## Решение 2: Передеплой через пустой коммит

Выполните в терминале:

```bash
cd /Users/rustamahatov/.cursor/my-project
git commit --allow-empty -m "Trigger redeploy"
git push
```

Это создаст пустой коммит и заставит Vercel передеплоить проект.

---

## Решение 3: Очистка кеша Vercel

### Через Vercel CLI (если установлен):

```bash
vercel --prod --force
```

### Или через Dashboard:
1. Settings → General
2. Найдите "Clear Build Cache"
3. Нажмите "Clear"

---

## Решение 4: Проверка что изменения применены

После передеплоя проверьте:

1. **Manifest endpoint:**
   ```
   https://three-in-a-row-nine.vercel.app/.well-known/farcaster.json
   ```
   
2. **Проверьте заголовки ответа:**
   ```bash
   curl -I https://three-in-a-row-nine.vercel.app/.well-known/farcaster.json
   ```
   
   Должны быть:
   - `Content-Type: application/json; charset=utf-8`
   - `Cache-Control: public, max-age=3600, must-revalidate`

3. **Проверьте что все поля присутствуют:**
   ```bash
   curl -s https://three-in-a-row-nine.vercel.app/.well-known/farcaster.json | grep -E "(version|name|iconUrl|homeUrl|splashImageUrl|splashBackgroundColor|primaryCategory|tags)"
   ```

---

## Решение 5: Очистка кеша Base Preview

1. Откройте [base.dev/preview](https://base.dev/preview)
2. Нажмите **Ctrl+Shift+R** (Windows) или **Cmd+Shift+R** (Mac) для жесткой перезагрузки
3. Или откройте в режиме инкогнито
4. Введите URL заново

---

## Проверка после передеплоя

1. Подождите 2-3 минуты после передеплоя
2. Проверьте manifest: `https://three-in-a-row-nine.vercel.app/.well-known/farcaster.json`
3. Проверьте на Base Preview: [base.dev/preview](https://base.dev/preview)
4. Убедитесь что все поля заполнены

---

## Если проблема остаётся

1. Проверьте логи Vercel на ошибки
2. Убедитесь что файл `app/.well-known/farcaster.json/route.ts` существует
3. Проверьте что все поля правильно экспортируются из `minikit.config.ts`
4. Попробуйте временно изменить одно из полей (например, version на "2") чтобы убедиться что изменения применяются

