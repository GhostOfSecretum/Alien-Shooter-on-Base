# 🚀 Автоматический деплой - Инструкция

Все готово для автоматического деплоя! Выполните следующие шаги:

## ✅ Что уже сделано

- ✅ Зависимости установлены
- ✅ Проект успешно компилируется
- ✅ Placeholder изображения созданы (SVG)
- ✅ Скрипты для деплоя готовы
- ✅ GitHub Actions workflow настроен

## 📋 Шаги для деплоя

### Вариант 1: Деплой через Vercel CLI (Рекомендуется)

1. **Установите Vercel CLI** (если еще не установлен):
   ```bash
   npm install -g vercel
   ```

2. **Войдите в Vercel**:
   ```bash
   vercel login
   ```

3. **Запустите скрипт деплоя**:
   ```bash
   ./deploy.sh
   ```
   
   Или вручную:
   ```bash
   npm run build
   vercel --prod
   ```

### Вариант 2: Деплой через GitHub + Vercel

1. **Создайте GitHub репозиторий**:
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Base Mini App - Три в ряд"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   git push -u origin main
   ```

2. **Импортируйте в Vercel**:
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите "Add New" → "Project"
   - Импортируйте ваш GitHub репозиторий
   - Нажмите "Deploy"

3. **Настройте GitHub Actions** (опционально):
   - Перейдите в Settings → Secrets вашего репозитория
   - Добавьте секреты:
     - `VERCEL_TOKEN` - получите на vercel.com/account/tokens
     - `VERCEL_ORG_ID` - найдите в настройках Vercel
     - `VERCEL_PROJECT_ID` - найдите в настройках проекта

### Вариант 3: Деплой через Vercel Dashboard

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New" → "Project"
3. Импортируйте репозиторий или загрузите файлы
4. Нажмите "Deploy"

## 🖼️ Создание изображений

### Быстрый способ (используя HTML генератор):

1. Откройте `public/generate-images.html` в браузере
2. Кликните на кнопки "Скачать" под каждым изображением
3. Сохраните PNG файлы в папку `public/`

### Альтернативный способ:

Используйте любой графический редактор (Figma, Canva, Photoshop) для создания:
- `icon.png` (512x512px)
- `hero.png` (1200x630px)
- `splash.png` (1080x1920px)
- `screenshot-portrait.png` (1080x1920px)
- `og-image.png` (1200x630px)

## ⚙️ После деплоя

1. **Обновите ROOT_URL**:
   - Откройте `minikit.config.ts`
   - Замените `ROOT_URL` на URL вашего деплоя:
     ```typescript
     const ROOT_URL = 'https://your-app.vercel.app';
     ```

2. **Отключите Deployment Protection**:
   - В настройках проекта Vercel
   - Settings → Deployment Protection
   - Отключите "Vercel Authentication"

3. **Создайте Account Association**:
   - Перейдите на [base.org/build/account-association](https://base.org/build/account-association)
   - Вставьте ваш домен
   - Следуйте инструкциям
   - Скопируйте `accountAssociation` объект

4. **Обновите minikit.config.ts**:
   ```typescript
   accountAssociation: {
     "header": "ваш-header",
     "payload": "ваш-payload",
     "signature": "ваша-signature"
   }
   ```

5. **Запушьте изменения**:
   ```bash
   git add .
   git commit -m "Update configuration"
   git push
   ```

6. **Проверьте приложение**:
   - Откройте [base.dev/preview](https://base.dev/preview)
   - Добавьте ваш URL
   - Проверьте все вкладки

7. **Опубликуйте**:
   - Создайте пост в Base app с URL вашего приложения

## 🎮 Проверка локально

Перед деплоем проверьте что все работает:

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) и проверьте:
- ✅ Игра запускается
- ✅ Можно делать ходы
- ✅ Счет работает
- ✅ Таблица лидеров работает
- ✅ Manifest доступен: http://localhost:3000/.well-known/farcaster.json

## 📝 Чеклист перед деплоем

- [ ] Зависимости установлены (`npm install`)
- [ ] Проект компилируется (`npm run build`)
- [ ] Локально все работает (`npm run dev`)
- [ ] Изображения созданы (или используйте SVG временно)
- [ ] ROOT_URL обновлен после деплоя
- [ ] Account Association настроен
- [ ] Deployment Protection отключен

## 🆘 Проблемы?

### Ошибка сборки
```bash
npm run build
```
Проверьте ошибки и исправьте их.

### Изображения не загружаются
- Убедитесь что файлы в папке `public/`
- Проверьте пути в `minikit.config.ts`
- Используйте абсолютные URL

### Manifest не работает
- Проверьте что файл `app/.well-known/farcaster.json/route.ts` существует
- Проверьте логи Vercel
- Убедитесь что ROOT_URL правильный

## 🎉 Готово!

После выполнения всех шагов ваша игра будет доступна в Base app!

